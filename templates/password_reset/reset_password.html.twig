{# templates/password_reset/reset_password.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Réinitialisation de mot de passe{% endblock %}

{% block body %}
    <main class="main">
        <div class="container">
            <section class="wrapper">
                <div class="heading">
                    <h1 class="text text-semi-large">Reset password</h1>
                </div>
                {{ form_start(form, {'attr': {'class': 'form'}}) }}
                {{ form_row(form.password, {'attr': {'class': 'input-field my-3'}}) }}

                <button type="submit" class="input-submit">Reset your password</button>
                {{ form_end(form) }}
            </section>
        </div>
    </main>
{% endblock %}
